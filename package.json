{
  "name": "share",
  "version": "0.1.0",
  "private": true,
  "description": "Instant HTML artifact sharing with AI-agent integration (Claude Code).",
  "license": "MIT",
  "author": "Fellipe Pinheiro",
  "workspaces": [
    "cli",
    "worker"
  ],
  "scripts": {
    "dev": "bun run --cwd worker dev",
    "deploy": "bun run --cwd worker deploy",
    "cli": "bun run --cwd cli start",
    "test:e2e": "bun run scripts/e2e.ts"
  }
}
